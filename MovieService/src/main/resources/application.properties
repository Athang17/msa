spring.application.name=MovieService

# Updated database name to match the schema we defined: movie_service
spring.datasource.url=jdbc:mysql://localhost:3306/movie_service?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
spring.datasource.username=root
spring.datasource.password=rootroot
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# Ensure Hibernate does not drop existing data and only updates the schema
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.database-platform=org.hibernate.dialect.MySQLDialect

server.port=8081